from django.conf.urls import url
from . import views

urlpatterns = [
    #url(r'^user/', views.user_profile_page_show),
    #url(r'^doctor/', views.doctor_profile_page_show),
    #url(r'^diagnostics/', views.diagnostics_profile_page_show),

]
